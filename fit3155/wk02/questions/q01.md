Describe using pseudocode, or otherwise, how to perform the preprocessing
required for the bad character rule.

```py
def rpreprocessing(pattern: str, string: str) -> None:
    n = len(string)
    m = len(pattern)

    # First create the array of the alphabet's size (assume 128 for ASCII chars)
    rarr = [0] * 128

    # Iterate through this array and set the positions of the rightmost occurrences for each character x in the pattern.;
    for i in range(0, m):
        rarr[ord(pattern[i])] = i
```