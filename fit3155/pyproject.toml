[project]
name = "fit3155"
version = "0.0.0"
requires-python = ">=3.10"

[tool.ruff]
line-length = 80

[tool.ruff.format]
docstring-code-format = true

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
